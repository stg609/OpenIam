@using Microsoft.AspNetCore.Identity
@using Charlie.OpenIam.Core.Models
@using IdentityModel
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="layui-nav layui-layout-right">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="layui-nav-item">
            <a href="javascript:;">
                <img src="http://t.cn/RCzsdCq" class="layui-nav-img">
                @User.Identity.Name
            </a>
            <dl class="layui-nav-child">
                <dd><a href="javascript:void(0);" class="uinfo" data-uid="@User.FindFirst(JwtClaimTypes.Subject)?.Value">基本资料</a></dd>
                <dd><a href="javascript:void(0);" class="pwd" data-uid="@User.FindFirst(JwtClaimTypes.Subject)?.Value">修改密码</a></dd>
            </dl>
        </li>

        <li class="layui-nav-item">
            <form name="logout" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <a href="javascript:document.logout.submit();">退 出</a>
            </form>
        </li>
    }
</ul>




